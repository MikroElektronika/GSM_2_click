unit Click_GSM_2_config;

uses Click_GSM_2_types;

const
    _GSM2_TIMER_LIMIT : uint32_t = 5; // 5 ticks
    _GSM2_BUF_WARNING : uint16_t = 192; // 192 bytes activate warning
    _GSM2_POLL_ENABLE : uint8_t = 1; // poll enabled
    _GSM2_CALLBACK_ENABLE : uint8_t = 0; // calback disabled
    _GSM2_UART_CFG : array[3] of uint32_t = (9600, _UART_NOPARITY, _UART_ONE_STOPBIT); 
    
end.